// Temporary internal data-entry screen — not part of the crawlable site, and
// depends on live auth + API, so it is served from the SPA fallback rather than
// prerendered.
export const prerender = false;
